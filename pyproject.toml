[project]
name = "es3d-l10n"
version = "0.1.3"
description = "Бесконечное лето 3D localization toolchain."
readme = "README.md"
license = "MIT"
requires-python = ">=3.12"
dependencies = [
    "rust-just>=1.55",
    "psutil>=6.0",
    "fonttools>=4.55",
]
